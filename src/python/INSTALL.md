To install this in your local Python you can use pip,
the recommended package manager for Python.

From this directory, enter the following command:

`pip install -e .`


If you are using PyPy3 you may have to use its version
of pip:

`pip_pypy3 install -e .`
